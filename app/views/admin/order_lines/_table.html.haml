%table#order_lines
  %thead
    %tr
      %th= Asset.model_name.human
      %th= Order.human_attribute_name :quantity
      %th= Order.human_attribute_name :unitary_price
      %th= Order.human_attribute_name :price

  %tbody
    - order.lines.each do |order_line|
      %tr{ :class => "#{cycle('even', 'odd')}" }
        %td= order_line.asset.name
        %td.number= order_line.quantity
        %td.currency= number_to_currency(order_line.unitaryPrice)
        %td.currency= number_to_currency(order_line.price)
