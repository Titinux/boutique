<table>
	<col width="100%" />
	<thead>
		<tr>
			<th><%= User.model_name.human %></th>
			<th><%= Asset.model_name.human %></th>
			<th><%= Deposit.human_attribute_name('quantity') %></th>
		</tr>
	</thead>
	<tbody>
		<%= render :partial => 'admin/deposits/row', :collection => deposits, :as => :deposit %>
	</tbody>
</table>
