<h2><%= t('order.new') %></h2>

<% form_for([:admin, @order]) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f, :bt_name => t('create') } %>
<% end %>

<%= link_to_back admin_orders_path %>
