<h1><%= t('order.editing') %></h1>

<% form_for([:admin, @order]) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f, :bt_name => t('update')} %>
<% end %>

<%= link_to t('show'), [:admin, @order] %> |
<%= link_to t('back'), admin_orders_path %>
