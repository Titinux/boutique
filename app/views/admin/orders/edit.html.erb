<h1>Editing order</h1>

<% form_for([:admin, @order]) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f, :bt_name => 'Update'} %>
<% end %>

<%= link_to 'Show', [:admin, @order] %> |
<%= link_to 'Back', admin_orders_path %>
