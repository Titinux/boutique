<h1>Editing user</h1>

<% form_for([:admin, @user]) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f, :bt_name => 'Update' } %>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', admin_users_path %>
