<p>
  Name : 
  <%=h @category.name %>
</p>
<p>
	Parent category : 
	<%= @category.parent ? h(@category.parent.name) : 'none' %>
</p>
<p>
	Picture uri : 
	<%=h @category.pictureUri %>
</p>

<%= link_to 'Edit', edit_admin_category_path(@category) %> |
<%= link_to 'Back', admin_categories_path %>
