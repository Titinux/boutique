<h1>New category</h1>

<% form_for([:admin, @category]) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f, :bt_name => 'Create' } %>
<% end %>

<%= link_to 'Back', admin_categories_path %>
