<% block :class => 'mini', :title => h(category.name) do %>
	<%= image_tag category.pictureUri unless category.pictureUri.blank? %>
	<%= link_to category.name, categories_path(:cat => category.id) %>
<% end %>
