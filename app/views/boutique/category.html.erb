<% unless @category.nil? %>
	<div id="category">
		<% block :title => h(@category.name) do %>
			<% unless @category.parent.blank? %>
				<p><%= link_to 'Go to parent category', categories_path(:cat => @category.parent.id) %></p>
			<% else %>
				<p><%= link_to 'Go to parent category', categories_path() %></p>
			<% end %>
			
			<%= render :partial => 'assets' , :locals => { :assets => @category.assets} %>
			<hr class="spacer" />
		<% end %>
	</div>
<% end %>

<div id="categories">
	<% @subCategories.each do |category| %>
		<%= render :partial => 'subCategories', :locals => { :category => category } %>
	<% end %>
	<hr class="spacer" />
</div>