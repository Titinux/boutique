en:
  activerecord:
    models:
      asset: Asset

    attributes:
      asset:
        name: Name
        category: Category
        pictureUri: Picture Uri

  # Asset
  asset:
    editing: Edit asset
    listing: Listing assets
    new: New asset
    showing: 'Asset : %{name}'
